#!/usr/bin/env node

'use strict';
process.title = 'homeeup';
var path = require('path');
var fs = require('fs');
var dist = path.join(path.dirname(fs.realpathSync(__filename)), '../dist');
require(dist + '/index');